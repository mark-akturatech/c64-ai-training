# - Fully Commented Commodore 64 ROM Disassembly (English) - Single-location jump to the illegal quantity error and warm start handler (JMP $B248). Used by string functions (e.g., MID$, ASC, byte-eval) when parameters or string length checks fail.

                                *** do illegal quantity error then warm start
.,B798 4C 48 B2 JMP $B248       do illegal quantity error then warm start


---
Additional information can be found by searching:
- "asc_function" which expands on ASC branches here on null string
- "mid_string_function" which expands on MID$ branches here on invalid indices/lengths
- "byte_parameter_parsing_and_evaluation" which expands on byte-evaluator may branch here on malformed byte expressions
