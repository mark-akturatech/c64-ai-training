# NMOS 6510 - Overview of unintended memory accesses: explanation that the 6502 always performs a memory access each execution cycle (reads/writes), that some accesses are dummy and can cause side effects when interacting with I/O registers, and mention that dummy fetches and writes can be (ab)used.

Read/Write

1

PC

Opcode fetch


---
Additional information can be found by searching:
- "read_modify_write_dummy_write_behavior" which expands on dummy writes in R-M-W instructions
