# ********* - EXIGNT ($ACFC) — ASCII text storage for input error messages: contains the strings '?EXTRA IGNORED' and '?REDO FROM START', each followed by a carriage return and a zero byte, used by INPUT/READ error reporting.


44284         $ACFC          EXIGNT
ASCII Text for Input Error Messages

The text stored here is ?EXTRA IGNORED and ?REDO FROM START, each
followed by a carriage return and a zero byte.

---
Additional information can be found by searching:
- "input_hash_statement" which expands on INPUT# behavior regarding EXTRA IGNORED
- "read_statement" which expands on READ/INPUT use these messages during error conditions
