# 6502 Microprocessor - STA pseudocode: STORE(address, src) — stores accumulator to memory address, used for all STA addressing forms.

/* STA */
    STORE(address, (src));

---
Additional information can be found by searching:
- "instruction_tables_sta" which expands on STA opcodes and modes
