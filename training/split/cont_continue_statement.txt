# ********* - CONT ($A857) — Perform CONT: restores the saved pointers to resume execution. If saved pointers are unavailable, prints CAN'T CONTINUE error.


43095         $A857          CONT
Perform CONT

The CONT statement is performed by moving the saved pointers back to
the current statement and current text character pointers.  If the
saved pointers cannot be retrieved, the CAN'T CONTINUE error statement
is printed.

---
Additional information can be found by searching:
- "end_statement_behavior" which expands on END saves pointers that CONT attempts to restore
