# ********* - GETSPA: routine to allocate space for a string. The requested amount is checked against available free memory; if insufficient, a garbage collection is triggered and allocation is retried.

46324         $B4F4          GETSPA
Allocate Space in Memory for String

The amount of space needed for a string is passed to this routine, and
the routine checks if there is that amount of space available in free
memory.  If not, it does a garbage collection and tries again.


---
Additional information can be found by searching:
- "garbag_string_garbage_collection_overview" which expands on invoked by GETSPA when space is insufficient
- "strlit_scan_and_setup" which expands on calls GETSPA to reserve space for literals
