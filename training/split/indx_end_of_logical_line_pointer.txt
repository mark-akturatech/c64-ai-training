# ********* - INDX ($C8) — Pointer (column number) to the end of the logical line for input. Explains that it holds the last nonblank character column index for an input logical line (range 0–79 because logical lines can be up to 80 characters).

200           $C8            INDX
Pointer: End of Logical Line for Input

This pointer indicates the column number of the last nonblank
character on the logical line that is to be input.  Since a logical
line can be up to 80 characters long, this number can range from 0-79.


---
Additional information can be found by searching:
- "lxsp_cursor_start_position" which expands on cursor start X,Y and logical line position used when performing input editing
- "pnt_pointer_and_pntr_cursor_column" which expands on screen line pointer and cursor column in relation to INDX
