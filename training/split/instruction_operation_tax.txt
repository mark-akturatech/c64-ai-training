# 6502 Microprocessor - TAX pseudocode: transfer A to X; set Sign and Zero based on copied value.

/* TAX */
    unsigned src = AC;
    SET_SIGN(src);
    SET_ZERO(src);
    XR = (src);


---
Additional information can be found by searching:
- "instruction_tables_tax" which expands on TAX opcode
