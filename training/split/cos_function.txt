# COMMODORE 64 - COS(<number>): returns cosine of number (angle in radians). Example converting degrees to radians COS(Y*{pi}/180).


  COS

  TYPE: Function
  FORMAT: COS (<number>)

    Action: This mathematical function calculates the cosine of the number,
  where the number is an angle in radians.

  EXAMPLES of COS Function:

    10 PRINT COS(0)
    20 X = COS(Y* {pi} /180) : REM CONVERT DEGREES TO RADIANS

---
Additional information can be found by searching:
- "sin_and_trig_functions" which expands on other trig functions such as SIN, TAN
