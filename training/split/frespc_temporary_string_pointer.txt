# ********* - FRESPC ($35-$36) — temporary pointer used by BASIC string routines. It holds a pointer to the most recently added or moved string and is used as a temporary working pointer while building or relocating string text.

53-54         $35-$36        FRESPC
Temporary Pointer for Strings

This is used as a temporary pointer to the most current string added
by the routines which build strings or move them in memory.


---
Additional information can be found by searching:
- "freetop_string_text_pointer" which expands on FRESPC is a temporary working pointer used when updating the string text area whose boundaries are managed by FREETOP and STREND
