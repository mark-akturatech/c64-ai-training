# C64 KERNAL API (Lee Davison) - $FFE1 — Scan the STOP key. If STOP is pressed when called the Z flag will be set; other flags unchanged. If not pressed accumulator will contain byte representing last row of keyboard scan. Can be used to check other special keys similarly.

$FFE1          scan the stop key
               
               If the STOP key on the keyboard is pressed when this routine is called the Z flag
               will be set. All other flags remain unchanged. If the STOP key is not pressed then
               the accumulator will contain a byte representing the last row of the keyboard scan.
               
               The user can also check for certain other keys this way.
               
               

---
Additional information can be found by searching:
- "ff9f_scan_the_keyboard" which expands on keyboard scanning routine
