# 6502 Microprocessor - LDA pseudocode: set Sign and Zero based on src and load AC = src.

/* LDA */
    SET_SIGN(src);
    SET_ZERO(src);
    AC = (src);


---
Additional information can be found by searching:
- "instruction_tables_lda" which expands on LDA opcodes and addressing modes
