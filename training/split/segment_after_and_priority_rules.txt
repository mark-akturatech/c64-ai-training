# Kick Assembler - Explains that startAfter uses the last defined memory block (default or user defined via *=). Notes blocks included by other methods (imported, included from files) are not considered when computing startAfter.

Segments

BasicUpstart2(start)
start:

sei
lda #00
tax
tay
jsr $1000

loop:


---
Additional information can be found by searching:
- "align_and_virtual_memory_blocks" which expands on alignment and virtual usage details
