# Architecture of 6502 Microprocessor - The second page ($0100–$01FF) is reserved for the system stack. It is used for storing return addresses and temporary values during subroutine calls and interrupts, and its location is fixed (cannot be moved).

The second page ($0100 to $01FF) is used to hold the system stack.  This is used to keep track of values, especially during subroutine calls.  It cannot be moved.


---
Additional information can be found by searching:
- "stack_pointer" which expands on stack pointer register behavior and stack mechanics
