# C64 KERNAL API (Lee Davison) - $FF9F — Scan the keyboard. Scans keyboard matrix and checks for pressed keys (same routine used by IRQ handler). If a key is down, its ASCII value is placed into the keyboard queue.

$FF9F          scan the keyboard
               
               This routine will scan the keyboard and check for pressed keys. It is the same
               routine called by the interrupt handler. If a key is down, its ASCII value is
               placed in the keyboard queue.
               
               

---
Additional information can be found by searching:
- "ffe4_get_character_from_input_device" which expands on keyboard input retrieval
- "ffea_increment_realtime_clock" which expands on IRQ vs polled scanning interplay
