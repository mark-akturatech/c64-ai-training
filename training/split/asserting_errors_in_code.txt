# Kick Assembler - Demonstrates .asserterror with code blocks to assert that given code produces an assembler error. Example: .asserterror "Test" , { lda #"This must fail" } and expected output showing type error message.

definition = new MacroDefinition();
definition.setName("MyMacro");
}
@Override
public MacroDefinition getDefinition() {
return definition;
}
@Override
