# ********* - STROUT ($AB1E) — Print string whose address is in A (low byte) and Y (high byte): routine outputs a zero-terminated string pointed to by .A and .Y using CHROUT for each character.


43806         $AB1E          STROUT
Print Message from a String Whose Address Is in the .Y and .A
Registers

This part of the PRINT routine outputs a string whose address is in
the Accumulator (low byte) and .Y register (high byte), and which ends
in a zero byte.

---
Additional information can be found by searching:
- "print_statement" which expands on STROUT is a subroutine within PRINT for string output
