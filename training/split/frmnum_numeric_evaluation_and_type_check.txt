# ********* - FRMNUM ($AD8A) — Evaluate numeric expression and/or check for data type mismatch: entry point(s) to check that a value matches the expected type (string vs numeric); produces TYPE MISMATCH if incompatible.


44426         $AD8A          FRMNUM
Evaluate a Numeric Expression and/or Check for Data Type Mismatch

This routine can be called from different entry points to check the
current data against the desired data type (string or numeric) to see
if they match.  If they don't, a TYPE MISMATCH error will result.

---
Additional information can be found by searching:
- "frmeval_expression_evaluation" which expands on FRMNUM is related to FRMEVAL routines for computing values
- "let_statement_variable_assignment" which expands on type checking during assignment
