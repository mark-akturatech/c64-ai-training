# ********* - RESTOR ($A81D) — Perform RESTORE: resets the DATA pointer (locations $41-$42) to the start-of-BASIC pointer (locations $2B-$2C) so subsequent READs begin from program start.


43037         $A81D          RESTOR
Perform RESTORE

The RESTORE command simply resetes the DATA pointer at 65-66 ($41-$42)
from the start of BASIC pointer at 43-44 ($2B-$2V).

---
Additional information can be found by searching:
- "read_statement" which expands on READ uses the DATA pointer that RESTORE resets
