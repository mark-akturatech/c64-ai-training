# NMOS 6510 - Table rows for the undocumented mnemonic SLO across multiple assemblers. Lists the opcode byte values for each addressing mode column for SLO as represented in KickAss, Acme, ca65, dasm and 64tass (including variants such as $07, $17, $03, $13, $0F, $1F, $1B and repeated assembler-specific entries).

SLO

$07 $17

$03 $13 $0F

$1F

$1B

SLO

SLO

SLO

SLO

SLO


---
Additional information can be found by searching:
- "opcode_naming_table_header" which expands on table format and assembler columns
- "rla_mnemonic_mapping" which expands on next related illegal opcode RLA
