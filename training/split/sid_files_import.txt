# Kick Assembler - Notes sid files can be imported as memory blocks using 'sidFiles' parameter. Example: .segment Main [sidFiles="data/music.sid", outPrg="out.prg"].

.label CHARSET = $3000
*=$0801 "Basic"
BasicUpstart(ch2)

ch4:

ch2:

---
Additional information can be found by searching:
- "sid_example_starter_code" which expands on example usage of sid imports
