# Sprite Multiplexing Techniques by Cadaver (Lasse Oorni) - Overview: Two basic methods for deciding which physical sprite registers to write and when—(1) just before the new sprite starts and (2) just after the old sprite ends. Either can be precalculated or done on-the-fly in the raster IRQs.

2.4 Determining what physical sprites to write, and when.

This can be done either "on the fly" in the raster interrupt or be
precalculated. There are two basic methods, see which one fits you better.


---
Additional information can be found by searching:
- "write_registers_just_before_new_sprite" which expands on method 1: write shortly before new sprite Y
- "write_registers_just_after_old_sprite" which expands on method 2: write right after previous sprite display
