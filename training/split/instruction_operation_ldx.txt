# 6502 Microprocessor - LDX pseudocode: set Sign and Zero and XR = src.

/* LDX */
    SET_SIGN(src);
    SET_ZERO(src);
    XR = (src);


---
Additional information can be found by searching:
- "instruction_tables_ldx" which expands on LDX opcodes
