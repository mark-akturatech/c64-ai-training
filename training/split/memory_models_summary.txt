# ca65 Users Guide - Memory model summary: default address sizes depend on chosen memory model (near/far/huge), switching memory model adjusts default address size for segments and thus for labels declared in them.

8.4 Memory models
The default address size of a segment depends on the memory model used. Since labels inherit the address size from the segment they are declared in, changing the memory model is an easy way to change the address size of many symbols at once.


---
Additional information can be found by searching:
- "address_sizes_and_memory_models" which expands on address-size inheritance and memory models
