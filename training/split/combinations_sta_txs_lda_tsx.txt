# NMOS 6510 - TAS/LAS behaviors: TAS {addr} stores A & X into SP and stores A & X & H into memory; LAS {addr} loads A,X,SP from memory & SP (A,X,SP = {addr} & SP). These are combinations involving stack pointer operations and masked stores/loads.

no effect

NOP #{imm} Fetches #{imm} but has no effects.
NOP {addr} Fetches {addr} but has no effects.

Lock-up
Opcode
JAM

---
Additional information can be found by searching:
- "unintended_opcodes_overview" which expands on TAS/LAS entries
