# COMMODORE 64 - Method: Use an editor/assembler package to assemble machine code; advantages similar to monitor but with easier entry and debugging. Disadvantages similar to monitor (load machine code from tape/disk when starting BASIC).

  3) EDITOR/ASSEMBLER PACKAGE:

    Advantages are similar to using a machine language monitor, but
  programs are even easier to enter. Disadvantages are also similar to the
  use of a machine language monitor.

---
Additional information can be found by searching:
- "machine_monitor_method" which expands on Both provide better entry/debugging than DATA statements
