# Kick Assembler - Stack and index push/pop instructions added on 65c02: PHX, PHY, PLX, PLY with their opcodes ($da, $5a, $fa, $7a). Includes trailing blank line.

phx

$da

phy

$5a

plx

$fa

ply

$7a


---
Additional information can be found by searching:
- "ora_instruction_encodings" which expands on instructions that manipulate accumulator/index registers in code sequences
- "rmb_instructions" which expands on stack usage patterns when manipulating memory bits or handling interrupts
