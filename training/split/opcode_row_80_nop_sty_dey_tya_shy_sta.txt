# NMOS 6510 - Opcode decode row for base 0x80. Contains a NOP immediate variant, STY (zp and abs and zp,x), DEY, BCC relative, TYA, and the undocumented SHY (abs,x) entry. Also covers STA variants across addressing modes: (zp,x), zp, #imm placeholder, abs, (zp),y, zp,x, abs,y, abs,x. Notes presence of undocumented/store-related instructions and filler NOPs.

80

NOP
#imm

STY
zp

DEY

STY
abs

BCC
rel

STY
zp,x

TYA

SHY
abs,x

STA
(zp,x)

STA
zp

NOP
#imm

STA
abs

STA
(zp),y

STA
zp,x

STA
abs,y

STA
abs,x


---
Additional information can be found by searching:
- "opcode_row_00_brk_php_bpl_ora" which expands on BRK / PHP / BPL / ORA group (base 0x00)
- "opcode_row_20_jsr_plp_bit_and" which expands on JSR / PLP / BIT / AND group (base 0x20)
- "opcode_row_40_rti_pha_jmp_bvc_eor" which expands on RTI / PHA / JMP / EOR group (base 0x40)
- "opcode_row_60_rts_pla_jmpind_bvs_adc" which expands on RTS / PLA / JMP (ind) / ADC group (base 0x60)
- "opcode_row_a0_ldy_tya_lda" which expands on LDY / TAY / LDA group (base 0xA0)
- "opcode_row_c0_cpy_cmp" which expands on CPY / INY / CMP group (base 0xC0)
- "opcode_row_e0_cpx_sbc" which expands on CPX / INX / SBC group (base 0xE0)
