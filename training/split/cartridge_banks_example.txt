# Kick Assembler - Example assembling cartridge banks into a single binary. Shows .segment CARTRIDGE_FILE [outBin="myfile.bin"] with .segmentout for BANK1..BANK4 and .segmentdef BANK1..BANK4 [min=$1000, max=$1fff, fill], then code sections for each BANKx restricted to $1000-$1fff.

lda #2
sta $d021
rts

// Start of memoryblock 3

A segment is a list of memory blocks. Since you haven't selected any segment in the above code, they are all
placed on the 'Default' segment.
A segment is defined by the .segmentdef directive and you use the .segment directive to decide which segment
to add code to:
// Define two segments

---
Additional information can be found by searching:
- "segments_introduction_and_overview" which expands on alternative outputs using segments
