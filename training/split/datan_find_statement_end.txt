# ********* - DATAN ($A906) — Search program text for the end of the current BASIC statement: starts at the current byte and searches until it finds a zero (line delimiter) or a colon not in quotes (statement delimiter).


43270         $A906          DATAN
Search Program Text for the End of the Current BASIC Statement

This routine starts at the current byte of program text and searches
until it finds a zero character (line delimiter) or a colon character
that is not in quotes (statement delimiter).

---
Additional information can be found by searching:
- "data_statement" which expands on DATAN used by DATA to find the next statement offset
- "rem_statement" which expands on REM also skips to the next statement delimiter
