# Kick Assembler - The general .string() function to obtain the string representation of any value. Example: .print 1234.string().charAt(2) — demonstrates chaining .string() with string methods (prints '3' for that example).


You can get the string representation of an arbitrary value by using the general .string() function. Eg.
.print 1234.string().charAt(2)

// Prints 3


---
Additional information can be found by searching:
- "string_methods_overview" which expands on Once you have a string (via .string()), you can use methods like charAt and size
- "numeric_to_string_functions" which expands on Alternate formatting helpers when converting numbers to strings
