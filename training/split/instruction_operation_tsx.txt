# 6502 Microprocessor - TSX pseudocode: transfer S (stack pointer) to X; set Sign and Zero accordingly.

/* TSX */
    unsigned src = SP;
    SET_SIGN(src);
    SET_ZERO(src);
    XR = (src);

---
Additional information can be found by searching:
- "instruction_tables_tsx" which expands on TSX opcode
