# Commodore 64 KERNAL Functions Reference - RAMTAS ($FF87). Clears memory ranges $0002-$0101 and $0200-$03FF, performs a memory test, and configures BASIC workspace and datasette buffer at $033C. No input/output parameters. Uses A, X, Y. Real address: $FD50.

$FF87 - RAMTAS
Clears memory ranges $0002-$0101 and $0200-$03FF, performs memory test, configures BASIC workspace and datasette buffer at $033C. No input/output parameters. Uses A, X, Y registers. Real address: $FD50.


---
Additional information can be found by searching:
- "memtop" which expands on BASIC workspace upper boundary (MEMTOP $FF99)
- "membot" which expands on BASIC workspace lower boundary (MEMBOT $FF9C)
