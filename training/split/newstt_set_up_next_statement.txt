# ********* - NEWSTT ($A7AE) — Tests for the STOP key, updates the pointer to the current line number, and positions the text pointer to read the beginning of the next BASIC statement.

42926         $A7AE          NEWSTT
Set Up Next Statement for Execution

This routine tests for the STOP key, updates the pointer to the
current line number, and positions the text pointer to read the
beginning of the statement.


---
Additional information can be found by searching:
- "stop_key_break_test" which expands on STOP key checking and break handling
- "gone_read_execute_next_statement" which expands on statement dispatch after NEWSTT positions text pointer
