# 6502 Microprocessor - EOR pseudocode: src ^= AC; set Sign and Zero; AC = result (bitwise exclusive-or).

/* EOR */
    src ^= AC;
    SET_SIGN(src);
    SET_ZERO(src);
    AC = src;

---
Additional information can be found by searching:
- "instruction_tables_eor" which expands on EOR opcodes
