# COMMODORE 64 - Insert Mode (part 1): explanation that spaces created by INST/DEL share characteristics with quote mode (cursor and color controls show as reversed characters) and that INST and DEL behave differently — INST performs normal insertion while DEL may create a special character in quote mode. Leads into examples of DEL/INST interactions.

  4. Insert Mode

    The spaces created by using the <INST/DEL> key have some of the same
  characteristics as quote mode. The cursor controls and color controls
  show up as reversed characters. The only difference is in the <INST> and
  <DEL>, which performs its normal function even in quote mode, now











---
Additional information can be found by searching:
- "quote_mode_color_controls_list_and_example" which expands on color and cursor controls also appear reversed inside insert-mode spaces
- "quote_mode_insert_mode_part2_and_warning" which expands on continued behavior, example using DEL/INST, and the warning about editing lines containing these characters
