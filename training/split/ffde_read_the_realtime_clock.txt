# C64 KERNAL API (Lee Davison) - $FFDE — Read the real-time clock. Returns current time in jiffies in A,X,Y with A being the most significant byte.

$FFDE          read the real time clock
               
               This routine returns the time, in jiffies, in AXY. The accumulator contains the
               most significant byte.
               
               

---
Additional information can be found by searching:
- "ffdb_set_the_realtime_clock" which expands on set clock value
