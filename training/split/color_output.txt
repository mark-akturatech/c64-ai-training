# ca65 Users Guide - The --color option: controls whether the assembler uses colored diagnostics; default is 'auto' which enables colors when output is a terminal.

--color
This option controls if the assembler will use colors when printing diagnostics. The default is "auto" which will enable colors if the output goes to a terminal (not to a file).



---
Additional information can be found by searching:
- "verbose_option" which expands on verbosity and diagnostic output options
