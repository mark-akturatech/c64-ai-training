# COMMODORE 64 - Explains LINE 300: the subroutine that controls the second sound effect (Voice 2). Shows POKES+11,129 to turn the waveform control on and POKES+11,128 to turn it off, with RETURN to resume execution at the caller (end of line 75).


  LINE 300:

    POKES+11,129        Waveform control set to 129 turns on the sound
                        effect.
    POKES+11,128        Waveform control set to 128 turns off the sound
                        effect.
    RETURN              Sends program back to end of line 75 to resume.

---
Additional information can be found by searching:
- "sprite_pointer_sound_triggers_gosub200_and_gosub300" which expands on this subroutine is invoked when P=193 (IF P=193 THEN GOSUB300)
- "waveform_control_subroutine_line_200" which expands on the first voice/subroutine which uses POKES+4 to control its waveform
