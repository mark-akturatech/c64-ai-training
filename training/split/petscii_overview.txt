# C64 PETSCII and Screen Codes Reference - sta.c64.org - Overview of PETSCII: definition (PET Standard Code of Information Interchange), used by the Commodore 64 and other Commodore computers; explains that the table lists all 256 PETSCII codes with decimal/hex values and character descriptions; notes how the character column shows both uppercase/graphics (up/gfx) and lowercase/uppercase (lo/up) modes.

PETSCII (PET Standard Code of Information Interchange) is the character
encoding used by the Commodore 64 and other Commodore computers.

The table below lists all 256 PETSCII codes with their decimal value,
hexadecimal value, and character description. The character column shows
the character in both character set modes: uppercase/graphics (up/gfx)
and lowercase/uppercase (lo/up), separated by a slash where they differ.


---
Additional information can be found by searching:
- "petscii_notes" which expands on special notes about control ranges and unused codes
- "petscii_to_screen_conversion" which expands on how PETSCII maps to screen codes used in video memory
- "screen_codes_intro" which expands on screen codes used in $0400-$07E7
