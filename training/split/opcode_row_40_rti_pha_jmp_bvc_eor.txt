# NMOS 6510 - Opcode decode row for base 0x40. Covers RTI, NOP (zp placeholder), PHA, JMP (abs), BVC relative, CLI, and the EOR family across addressing modes: (zp,x), zp, #imm, abs, (zp),y, zp,x, abs,y, abs,x. Shows the common NOP/placeholder positions and standard flag/branch/control instructions in this row.

40

RTI

NOP
zp

PHA

JMP
abs

BVC
rel

NOP
zp,x

CLI

NOP
abs,x

EOR
(zp,x)

EOR
zp

EOR
#imm

EOR
abs

EOR
(zp),y

EOR
zp,x

EOR
abs,y

EOR
abs,x


---
Additional information can be found by searching:
- "opcode_row_00_brk_php_bpl_ora" which expands on BRK / PHP / BPL / ORA group (base 0x00)
- "opcode_row_20_jsr_plp_bit_and" which expands on JSR / PLP / BIT / AND group (base 0x20)
- "opcode_row_60_rts_jmpind_adc" which expands on RTS / JMP (indirect) / ADC group (base 0x60)
- "opcode_row_80_nop_sty_sta" which expands on NOP / STY / STA group (base 0x80)
- "opcode_row_a0_ldy_tya_lda" which expands on LDY / TAY / LDA group (base 0xA0)
- "opcode_row_c0_cpy_cmp" which expands on CPY / INY / CMP group (base 0xC0)
- "opcode_row_e0_cpx_sbc" which expands on CPX / INX / SBC group (base 0xE0)
