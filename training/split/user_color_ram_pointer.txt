# ********* - USER ($F3-$F4) — Pointer to the address of the current screen color RAM location. Explains that this pointer is synchronized with PNT ($D1) and holds the address of the first byte of color RAM for the corresponding screen line.

243-244       $F3-$F4        USER
Pointer to the Address of the Current Screen Color RAM Location

This poitner is synchronized with the pointer to the address of the
first byte of screen RAM for the current line kept in location 209
($D1).  It holds the address of the first byte of color RAM for the
corresponding screen line.


---
Additional information can be found by searching:
- "pnt_pointer_and_pntr_cursor_column" which expands on synchronization between the screen RAM pointer and the color-RAM pointer
