# NMOS 6510 - Statement that ADC and SBC (and undocumented instructions derived from them: ARR, RRA, ISC and the undocumented $EB SBC) are affected by the decimal flag. References test code scanner.

i

o

x

---
Additional information can be found by searching:
- "arr_decimal_mode" which expands on ARR behaviour in decimal mode
- "rra_instruction" which expands on RRA behaviour (inherits ADC decimal mode)
