# Kick Assembler - Describes the .namespace directive. Example: .namespace vic { .label borderColor = $d020 .label backgroundColor0 = $d021 .label backgroundColor1 = $d022 .label backgroundColor2 = $d023 }. Explains that declaring a namespace places a symbol in the parent scope and labels inside are accessed as fields (vic.borderColor etc).

...
rts

9.5. Escaping the current scope or namespace
To escape the current scope, use @ to reference the root scope. In the following code '@myLabel' access the
myLabel label in the root scope:
.label myLabel = 1
{
.label myLabel = 2
.print "scoped myLabel="+ myLabel //<-- Returns 2
.print "root myLabel="+ @myLabel //<-- Returns 1
}

The same can be done for functions, macros and pseudo commands. So the following example will print 'root'
not 'mySpace':

---
Additional information can be found by searching:
- "scope_hierarchy_and_symbol_resolution" which expands on scope resolution behavior
- "filenamespace_example_files" which expands on filenamespace for file-level namespaces
