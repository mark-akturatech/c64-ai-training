# byRiclianll - Assembler directives and equates: .OPT P,02 optimization directive, setting assembly origin (* = *0400) and defining IDL = *0431 and IDH = IDL+35 (per-track ID low/high table addresses).

.OPT  P,02 


*=  *0400 
IDL  =  *0431 
IDH  =  IDL+35 
5 

---
Additional information can be found by searching:
- "basic_preamble_open_sys" which expands on BASIC invocation that calls into the assembled code
- "vector_and_drive_initialization" which expands on Initialization of zero page and vectors that uses the origin and equates
