# byRiclianll - WAIT polling loop and final jump: WAIT LDA *03 ; BMI WAIT — repeatedly polls location $03 and loops on negative (BMI) until ready. Afterwards performs JMP *C194 (jump to ENDCMD). This implements a simple busy-wait for drive status before finishing.

WAIT  LDA  *03 

BMI  WAIT 

5 

JMP  *C194 

---
Additional information can be found by searching:
- "table_load_id_bytes_loop" which expands on TABLE loop prepares ID bytes prior to entering the WAIT/poll loop
- "inline_comments_id_labels" which expands on Data/labels for ID table entries referred to by the TABLE loop
