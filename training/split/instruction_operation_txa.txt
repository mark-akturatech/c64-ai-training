# 6502 Microprocessor - TXA pseudocode: transfer X to A; set Sign and Zero based on X value.

/* TXA */
    unsigned src = XR;
    SET_SIGN(src);
    SET_ZERO(src);
    AC = (src);

---
Additional information can be found by searching:
- "instruction_tables_txa" which expands on TXA opcode
