# Kick Assembler - Explains the 'Default' segment: if you don't use .segment code is placed on 'Default' segment which is connected to standard output file. To return to Default segment, use .segment Default.

lda text,x
cmp #$ff
beq out
sta $0400,x
inx
jmp loop
out:

---
Additional information can be found by searching:
- "segments_introduction_and_overview" which expands on default behavior without segments
