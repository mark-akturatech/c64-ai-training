# Architecture of 6502 Microprocessor - Decimal Mode Flag (D). When set, the processor performs Binary Coded Decimal (BCD) arithmetic rules during addition and subtraction. Set with SED and cleared with CLD.


Decimal Mode 		D

While the decimal mode flag is set the processor will obey the rules of Binary Coded Decimal (BCD) arithmetic during addition and subtraction. The flag can be explicitly set using 'Set Decimal Flag' (SED) and cleared with 'Clear Decimal Flag' (CLD).


---
Additional information can be found by searching:
- "processor_status_register" which expands on flag P contains the D bit
