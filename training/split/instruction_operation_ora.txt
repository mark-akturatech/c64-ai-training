# 6502 Microprocessor - ORA pseudocode: src |= AC; set Sign and Zero; AC = result (bitwise OR).

/* ORA */
    src |= AC;
    SET_SIGN(src);
    SET_ZERO(src);
    AC = src;

---
Additional information can be found by searching:
- "instruction_tables_ora" which expands on ORA opcodes and modes
