# 6502 Microprocessor - TAY pseudocode: transfer A to Y; set Sign and Zero based on copied value.

/* TAY */
    unsigned src = AC;
    SET_SIGN(src);
    SET_ZERO(src);
    YR = (src);

---
Additional information can be found by searching:
- "instruction_tables_tay" which expands on TAY opcode
