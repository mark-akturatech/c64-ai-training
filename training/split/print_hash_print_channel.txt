# ********* - PRINT# (PRINTN, $AA80) — Perform PRINT#: calls CMD to send text to a device and then closes the output channel using the Kernal CLRCHN routine ($F333).


43648         $AA80          PRINTN
Perform PRINT#

The PRINT# statement calls CMD and then closes the output channel with
the Kernal CLRCHN routine (62259, $F333).

---
Additional information can be found by searching:
- "cmd_statement_output_channel" which expands on PRINT# calls CMD to perform the output
- "print_statement" which expands on PRINT variants and formatting rules
