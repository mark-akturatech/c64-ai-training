# NMOS 6510 - Opcode decode row for base 0xE0. Covers CPX (#imm, zp, abs), INX, BEQ relative, SED, NOP placeholders, and the SBC family across addressing modes: (zp,x), zp, #imm, abs, (zp),y, zp,x, abs,y, abs,x. This row groups compare and subtract-with-carry operations and standard increments/flag-setting instructions.

E0

CPX
#imm

CPX
zp

INX

CPX
abs

BEQ
rel

NOP
zp,x

SED

NOP
abs,x

SBC
(zp,x)

SBC
zp

SBC
#imm

SBC
abs

SBC
(zp),y

SBC
zp,x

SBC
abs,y

SBC
abs,x


---
Additional information can be found by searching:
- "opcode_row_00_brk_php_bpl_ora" which expands on BRK / PHP / BPL / ORA group (base 0x00)
- "opcode_row_20_jsr_plp_bit_and" which expands on JSR / PLP / BIT / AND group (base 0x20)
- "opcode_row_40_rti_pha_jmp_bvc_eor" which expands on RTI / PHA / JMP / EOR group (base 0x40)
- "opcode_row_60_rts_pla_jmpind_bvs_adc" which expands on RTS / PLA / JMP (ind) / ADC group (base 0x60)
- "opcode_row_80_nop_sty_dey_tya_shy_sta" which expands on NOP / STY / STA group (base 0x80)
- "opcode_row_a0_ldy_tay_bcs_clv_lday_lda" which expands on LDY / TAY / LDA group (base 0xA0)
- "opcode_row_c0_cpy_iny_bne_cld_cmp" which expands on CPY / INY / CMP group (base 0xC0)
