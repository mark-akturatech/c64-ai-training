# Kick Assembler - Section 3.3: Supported numeric literal formats (decimal, hexadecimal with $, binary with %), notes about unknown/unresolved labels being treated as 16-bit values in early passes, and examples.


3.3. Number formats
Kick Assembler supports the standard number formats:
1

If the argument is unknown (eg. an unresolved label) in the first pass, the assembler will assume it’s a 16 bit value

5

Basic Assembler Functionality

Table 3.4. Number formats
Prefix

Format

Example

Decimal

lda #42

$

Hexadecimal

lda #$2a, lda #$ff

%

Binary

lda #%101010


---
Additional information can be found by searching:
- "numeric_values_and_operators" which expands on numeric operators, byte/high/low operators (>,<) and bitwise operations in script expressions
