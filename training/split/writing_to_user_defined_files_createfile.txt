# Kick Assembler - Explains createFile function to create/overwrite host files from script. Example: .var myFile = createFile("breakpoints.txt") .eval myFile.writeln("Hello World"). Note: for security you must use -afo switch when running Kick Assembler to allow file creation.

jsr source1.clearColor

12.7. Exporting Labels to VICE
By using the –vicesymbols option you can export the labels to a .vs file that can be read by the VICE emulator.
For example:
java –jar KickAss.jar source1.asm –vicesymbols

64


---
Additional information can be found by searching:
- "breakpoints_file_example" which expands on example writing breakpoints file
