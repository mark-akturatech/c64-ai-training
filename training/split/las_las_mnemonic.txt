# NMOS 6510 - Rows for the LAS mnemonic and its assembler variants (also shown as LAE or LDS in some assemblers). Lists opcode byte values (example $BB and other variants) and documents how LAS is represented across assemblers and addressing modes.

LAS

$BB

LAS

LAS

LAS

LAS

LAS, LAE, LDS


---
Additional information can be found by searching:
- "tas_shs_mnemonics" which expands on previous group (TAS/SHS)
- "lax_lxa_variants" which expands on adjacent LAX / LXA variants
