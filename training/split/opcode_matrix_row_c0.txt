# NMOS 6510 - Opcode matrix row for C0..DF. Contains NOP (#imm), DEC (zp, abs, zp,x, abs,x), DEX, and undocumented/combined opcodes DCP with addressing modes (zp,x), zp, abs, (zp),y, zp,x, abs,y, abs,x. Also includes the undocumented immediate SBX entry.

C0

NOP
#imm

DEC
zp

DEX

DEC
abs

JAM

DEC
zp,x

NOP

DEC
abs,x

DCP
(zp,x)

DCP
zp

SBX
#imm

DCP
abs

DCP
(zp),y

DCP
zp,x

DCP
abs,y

DCP
abs,x


---
Additional information can be found by searching:
- "opcode_matrix_row_a0" which expands on previous opcode row (A0..BF) with LDX/LAX
- "opcode_matrix_row_e0" which expands on next opcode row (E0..FF) with INC/ISC/SBC
- "opcode_matrix_notes_and_labels" which expands on block labels and concluding note about variant grouping
