# C64 KERNAL API (Lee Davison) - $FFE7 — Close all channels and files. Closes all open files and resets pointers into the open file table; also resets I/O channels to defaults.

$FFE7          close all channels and files
               
               This routine closes all open files. When this routine is called, the pointers into
               the open file table are reset, closing all files. Also the routine automatically
               resets the I/O channels.
               
               

---
Additional information can be found by searching:
- "ffcc_close_input_and_output_channels" which expands on close individual I/O channels
