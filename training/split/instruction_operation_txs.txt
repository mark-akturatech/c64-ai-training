# 6502 Microprocessor - TXS pseudocode: transfer X to stack pointer (SP = X). Does not affect flags.

/* TXS */
    unsigned src = XR;
    SP = (src);

---
Additional information can be found by searching:
- "instruction_tables_txs" which expands on TXS opcode
