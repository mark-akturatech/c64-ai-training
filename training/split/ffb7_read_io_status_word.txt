# C64 KERNAL API (Lee Davison) - $FFB7 — Read I/O status word. Returns current status of I/O device in A (status/errors). Typically called after serial communication to determine device status or errors that occurred during I/O operations.

$FFB7          read I/O status word
               
               This routine returns the current status of the I/O device in the accumulator. The
               routine is usually called after new communication to an I/O device. The routine
               will give information about device status, or errors that have occurred during the
               I/O operation.
               
               

---
Additional information can be found by searching:
- "ffa5_input_byte_from_serial_bus" which expands on errors after serial input
- "ffa8_output_byte_to_serial_bus" which expands on errors after serial output
- "ffa2_set_timeout_on_serial_bus" which expands on timeout flag affects status
