# Kick Assembler - Explains .asserterror to assert that an expression produces an error (useful for testing error handling). Example: .asserterror "Test1", 20/10 (will fail) .asserterror "Test2", 20/false (expected error due to invalid operand). Output distinguishes OK vs ERROR IN ASSERTION.

test.plugins.macros.MyMacro3

17.3. A quick Example (Macros)
First, let's see a quick example of an implemented plugin. To implement a macro plugin you must create a java
class that implements the IMacro interface:

77

3rd Party Java plugins

