# ********* - RUN ($A871) — Perform RUN: calls the Kernal SETMSG to set the RUN message flag, performs a CLR to start execution. If a line number follows RUN, a GOTO to that line is performed after CLR.


43121         $A871          RUN
Perform RUN

RUN is executed by calling the Kernal SETMSG (65048, $FE18) routine to
set the message flag for RUN mode and performing a CLR to start the
program.  If a line followed RUN, a GOTO is executed after the CLR.

---
Additional information can be found by searching:
- "goto_statement" which expands on RUN may perform a GOTO to a line after clearing
