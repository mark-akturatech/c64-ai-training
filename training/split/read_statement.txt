# ********* - READ ($AC06) — Perform READ: common code for READ, GET and INPUT. Locates the next DATA item, reads the text, converts it to the appropriate data type, and assigns to numeric or string variables.


44038         $AC06          READ
Perform READ

This routine includes the READ command and common code for GET and
INPUT.  The READ command locates the next piece of DATA, reads the
text, and converts it to the appropriate type of data to be assigned
to a numeric or string variable.

---
Additional information can be found by searching:
- "data_statement" which expands on READ consumes DATA laid out by DATA statements
- "doagin_io_error_formatting" which expands on error formatting routines used when READ encounters bad input
