# Kick Assembler - Shows that @ can be used to call root-scope functions/macros/pseudocommands from inside a namespace. Example: .function myFunction() { .return "root" } .namespace mySpace { .function myFunction() { .return "mySpace" } .print @myFunction() } — prints 'root'.

.return 1
}
.macro @MyMacro() {
.print "Macro Called"
}
.macro @MyPseudoCommand {
.print "PseudoCommand Called"
}

---
Additional information can be found by searching:
- "escape_root_scope_with_at" which expands on using @ to escape scope
