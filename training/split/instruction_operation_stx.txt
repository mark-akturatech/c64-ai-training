# 6502 Microprocessor - STX pseudocode: STORE(address, src) — stores X to memory address.

/* STX */
    STORE(address, (src));

---
Additional information can be found by searching:
- "instruction_tables_stx" which expands on STX opcodes
