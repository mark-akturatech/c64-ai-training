# Kick Assembler - Table 11.1: File directive parameters: mbfiles (if true create a file for each memory block), name (filename, %o substitution), type ("prg" or "bin").

The complete of possible parameters for the disk is:

Table 11.2. Disk parameters
Parameter

Default

Example

Description

dontSplitFilesOverDir

false

dontSplitFilesOverDir

If set to true, the file that
would otherwise have sectors on both sides of the directory track will be moved
to after the directory track.

55

PRG files and D64 Disks

Parameter

Default

filename

Example

