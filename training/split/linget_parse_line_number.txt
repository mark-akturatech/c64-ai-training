# ********* - LINGET ($A96B) — Convert an ASCII decimal number to a two-byte binary line number: parses a decimal ASCII number, produces a low-byte/high-byte two-byte integer, and checks that it lies within the valid 0–63999 BASIC line range.


43371         $A96B          LINGET
Convert an ASCII Decimal Number to a Two-Byte Binary Line Nmumber

This subroutine is used by several statements to read a decimal
number, convert it to a two-byte integer line number (in low-byte,
high-byte format), and check that it is in the correct range of
0-63999.

---
Additional information can be found by searching:
- "goto_statement" which expands on GOTO uses LINGET to parse target line numbers
- "on_goto_on_gosub_statement" which expands on ON GOTO/GOSUB uses LINGET for line numbers
