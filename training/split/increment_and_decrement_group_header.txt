# 6502 Instructions summary - Group header: 'Increment and Decrement Group' (section title for INC, INX, INY, DEC, DEX, DEY).

Increment and Decrement Group


---
Additional information can be found by searching:
- "inc_instruction" which expands on INC memory increment
- "inx_instruction" which expands on INX register increment
- "iny_instruction" which expands on INY register increment
- "dec_instruction" which expands on DEC memory decrement
- "dex_instruction" which expands on DEX register decrement
- "dey_instruction" which expands on DEY register decrement
