# ********* - PRINT ($AAA0) — Perform PRINT: the main output routine with segments handling TAB, SPC, comma, semicolon, variables, PI, ST, TI, TI$. All output is eventually converted to strings and emitted character-by-character via the Kernal CHROUT routine.


43680         $AAA0          PRINT
Perform PRINT

The PRINT routine has many segments, which are required for the
various options which can be added to it:  TAB, SPC, comman,
semicolon, variables, PI, ST, TI, and TI$.  Eventually, all output is
converted to strings, and the Kernal CHROUT routine is called to print
each character.

---
Additional information can be found by searching:
- "strout_string_output" which expands on STRING output helper used by PRINT
- "cmd_statement_output_channel" which expands on CMD uses PRINT for actual output
