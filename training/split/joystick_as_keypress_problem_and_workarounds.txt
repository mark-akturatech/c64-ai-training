# ********* - Notes the harder problem of mistaking joystick movement for keyboard keypresses (and vice versa). Typical workaround: use Controller Port 2 for games to avoid conflicts; anecdote about using joystick left to slow program listing (interpreted as CTRL by keyscan).

conflict.  So, if you can't beat them, sit back and press your
joystick to the left in order to slow down a program listing (the
keyscan routine thinks that it is the CTRL key).

---
Additional information can be found by searching:
- "keyboard_joystick_confusion_and_keyscan_disable" which expands on turning off keyscan to isolate joystick reads
