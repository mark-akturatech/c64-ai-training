# Kick Assembler - Explains how Kick Assembler resolves symbols through nested scopes. Shows a small code fragment and states the resulting scope hierarchy (System <- Root <- BracketScope1 <- BracketScope2) and how 'jmp loop' resolves to the innermost label while 'jmp start' resolves to the root scope label.

rts
exec:
...
rts

/* FILE 2 */
.filenamespace part2
init:
...
rts
exec:

---
Additional information can be found by searching:
- "namespace_directive_and_namespace_field_access" which expands on namespaces are related but resolved similarly
