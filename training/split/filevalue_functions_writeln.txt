# Kick Assembler - Table of FileValue functions: writeln(text) writes text and newline, writeln() inserts blank line. Used on values returned by createFile.

inc $d020
dec $d021
jmp main
*=$1000
.fill $100, i
}

65

Chapter 14
Special Features
Misc features

14.1. Name and path of the sourcefile
You can get the filename and the path of the current sourcefile with the getPath() and getFilename() functions:
.print "Path : " + getPath()
.print "Filename : " + getFilename()

