# Commodore 64 KERNAL Functions Reference - PLOT ($FFF0). Manages cursor position. Input: Carry (0 = restore, 1 = save); X/Y = column/row if Carry=0. Output: X/Y = column/row if Carry=1. Uses X, Y registers. Real address: $E50A.

$FFF0 - PLOT
Manages cursor position. Input: Carry (0=restore, 1=save); X/Y = column/row if Carry=0. Output: X/Y = column/row if Carry=1. Uses X, Y registers. Real address: $E50A.


---
Additional information can be found by searching:
- "screen" which expands on screen dimensions (SCREEN $FFED)
